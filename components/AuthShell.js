import Image from "next/image";
import Link from "next/link";
export default function AuthShell({ title, subtitle, children, footer }) { return <div className="flex min-h-screen flex-col items-center justify-center bg-surface px-5 py-12 text-ink"><Link href="/" className="mb-10 flex items-center gap-3"><Image src="/brand/sella-mark.png" alt="" width={32} height={32} className="h-8 w-8 rounded-xl" /><span className="text-[15px] font-semibold">Sella</span></Link><div className="w-full max-w-md rounded-2xl border border-line bg-white p-8"><p className="text-[13px] font-medium tracking-[0.02em] text-muted">Sella / access</p><h1 className="display mt-4 text-[40px] leading-[1.05]">{title}</h1>{subtitle && <p className="mt-4 text-[15px] leading-7 text-muted">{subtitle}</p>}<div className="mt-8">{children}</div></div>{footer && <p className="mt-6 text-[15px] text-muted">{footer}</p>}</div>; }

"use client";
import { useState } from "react";
export default function SubscriptionForm({ storeId, plan, amount }) { const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); async function submit() { setBusy(true); setMessage(""); const response = await fetch("/api/subscriptions", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ storeId, plan, amount, paidWith: "transfer" }) }); const data = await response.json().catch(() => ({})); setBusy(false); setMessage(response.ok ? "Payment instructions requested" : data.error || "Unable to start plan"); } return <div><button type="button" className="btn-primary w-full" onClick={submit} disabled={busy}>{busy ? "Starting…" : "Choose plan"}</button>{message && <p className="mt-2 text-xs text-muted">{message}</p>}</div>; }

"use client";
import { useState } from "react";
import Link from "next/link";
export default function FollowStoreButton({ storeId, initialFollowing = false, returnTo }) { const [following, setFollowing] = useState(initialFollowing); const [message, setMessage] = useState(""); async function toggle() { setMessage(""); const response = await fetch("/api/buyer/follow", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ storeId, follow: !following }) }); const data = await response.json().catch(() => ({})); if (response.status === 401) { const next = `/account/setup?returnTo=${encodeURIComponent(returnTo || "/account")}`; return setMessage(<Link href={`/signup?next=${encodeURIComponent(next)}`} className="text-[13px] font-medium text-kola underline">Create a buyer account to follow</Link>); } if (!response.ok) return setMessage(data.error || "Unable to update follow"); setFollowing(data.following); } return <div className="flex flex-wrap items-center gap-3"><button type="button" onClick={toggle} className={following ? "btn-secondary" : "btn-primary"}>{following ? "Following" : "Follow store"}</button>{message && <span className="text-[13px] text-muted">{message}</span>}</div>; }

"use client";
import { useState } from "react";
export default function AdminSettingForm({ initialRate, settingKey = "commission_rate", label = "Save rate" }) { const [rate, setRate] = useState(initialRate); const [message, setMessage] = useState(""); const isThreshold = settingKey === "trusted_order_threshold"; async function submit(event) { event.preventDefault(); const response = await fetch("/api/admin", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ action: "setting", key: settingKey, value: isThreshold ? { count: Number(rate) } : { rate: Number(rate) } }) }); const data = await response.json().catch(() => ({})); setMessage(response.ok ? "Saved" : data.error || "Save failed"); } return <form onSubmit={submit} className="mt-4 flex flex-wrap items-end gap-3"><div><label className="label">{isThreshold ? "Delivered orders" : "Rate %"}</label><input className="input w-32" type="number" min="0" max={isThreshold ? "1000" : "100"} step={isThreshold ? "1" : "0.1"} value={rate} onChange={(event) => setRate(event.target.value)} /></div><button className="btn-primary">{label}</button>{message && <span className="text-sm text-muted">{message}</span>}</form>; }

"use client";
import { useState } from "react";
export default function AdminSettingForm({ initialRate }) { const [rate, setRate] = useState(initialRate); const [message, setMessage] = useState(""); async function submit(event) { event.preventDefault(); const response = await fetch("/api/admin", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ action: "setting", key: "commission_rate", value: { rate: Number(rate) } }) }); const data = await response.json().catch(() => ({})); setMessage(response.ok ? "Commission saved" : data.error || "Save failed"); } return <form onSubmit={submit} className="mt-4 flex flex-wrap items-end gap-3"><div><label className="label">Rate %</label><input className="input w-32" type="number" min="0" max="100" step="0.1" value={rate} onChange={(event) => setRate(event.target.value)} /></div><button className="btn-primary">Save rate</button>{message && <span className="text-sm text-muted">{message}</span>}</form>; }

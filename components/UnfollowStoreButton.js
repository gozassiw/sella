"use client";
import { useState } from "react";
export default function UnfollowStoreButton({ storeId }) { const [busy, setBusy] = useState(false); const [error, setError] = useState(""); async function unfollow() { setBusy(true); setError(""); try { const response = await fetch("/api/buyer/follow", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ storeId, follow: false }) }); const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.error || "Unable to remove this store."); window.location.reload(); } catch (caught) { setError(caught.message || "Unable to remove this store."); setBusy(false); } } return <span className="inline-flex flex-col items-end gap-1"><button type="button" onClick={unfollow} disabled={busy} className="text-xs font-semibold text-red-700 hover:underline">{busy ? "Removing…" : "Unfollow"}</button>{error && <span className="max-w-40 text-right text-[11px] text-danger">{error}</span>}</span>; }

import Link from "next/link";
import { BadgeCheck } from "lucide-react";
import { getMyStore } from "@/lib/store";
import PaidVerificationForm from "@/components/PaidVerificationForm";

export default async function VerificationBadgePage() {
  const { supabase, store } = await getMyStore();
  await supabase.rpc("sync_store_verification_status", { p_store_id: store.id });
  const [{ data: settings }, { data: wallet }, { data: purchases }] = await Promise.all([
    supabase.rpc("get_paid_verification_settings"),
    supabase.from("wallets").select("available").eq("store_id", store.id).maybeSingle(),
    supabase.from("store_verification_purchases").select("id,status,amount,paid_at,reviewed_at,expires_at,payment_account_number,payment_account_name,payment_bank_name,payment_account_expires_at,payment_reference,created_at").eq("store_id", store.id).order("created_at", { ascending: false }).limit(20),
  ]);
  const purchaseList = purchases || [];
  const activeApprovedPurchase = purchaseList.find((item) => item.status === "approved" && item.expires_at && new Date(item.expires_at) > new Date());
  const purchase = activeApprovedPurchase || purchaseList[0] || null;
  const price = Number(settings?.amount || 0);
  const approvedAndActive = Boolean(activeApprovedPurchase);
  const currentStatus = approvedAndActive ? "approved" : purchase?.status === "paid" ? "paid" : purchase?.status === "approved" ? "expired" : purchase?.status === "pending" ? "pending_review" : null;
  const purchaseForForm = purchase ? { ...purchase, amount: Number(purchase.amount), accountNumber: purchase.payment_account_number, accountName: purchase.payment_account_name, bankName: purchase.payment_bank_name, paymentAccountExpiresAt: purchase.payment_account_expires_at, reference: purchase.payment_reference } : null;
  return <div className="space-y-6"><div><p className="eyebrow text-[#2563EB]">Separate seller tool</p><h1 className="display mt-2 text-3xl">Verification checkmark</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted">This is its own six-month verification purchase, separate from Basic, Plus, and Premium plans. Admin approval is required before the checkmark and verified-store discovery visibility activate.</p></div><section className="panel max-w-2xl"><div className="flex items-start gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-blue-50 text-[#2563EB]"><BadgeCheck size={23} fill="currentColor" /></span><div><h2 className="font-extrabold">What makes this different from a normal plan?</h2><p className="mt-2 text-sm leading-6 text-muted">Basic, Plus, and Premium keep your store open and let you receive orders; they are your store subscription. The verification checkmark is a separate trust and discovery add-on, so it does not replace your active plan or change your plan&apos;s commission rate.</p><p className="mt-3 text-sm leading-6 text-muted">After Sella approves the review, a blue checkmark appears beside your store name and your store becomes eligible to appear in Sella&apos;s buyer discovery feed, including for buyers who have not already trusted your store. This gives buyers a clearer signal that Sella has reviewed the store and makes your business easier to recognise.</p></div></div><div className="mt-5 rounded-2xl bg-blue-50 p-4 text-sm leading-6 text-[#1D4ED8]"><p className="font-extrabold">The checkmark is not automatic</p><p className="mt-1">Paying submits your store for a separate review. The badge and discovery visibility activate only after Admin approval and stop when the six-month verification period expires.</p></div><div className="mt-6 flex items-start gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-blue-50 text-[#2563EB]"><BadgeCheck size={23} fill="currentColor" /></span><div><h2 className="font-extrabold">Get Sella&apos;s verification checkmark</h2><p className="mt-2 text-sm leading-6 text-muted">The current price is billed every 6 months from the date of payment. Your price is controlled by Sella Admin and may change for a future renewal. Approved verification expires automatically if the next six-month payment is not completed.</p></div></div>{price > 0 ? <><p className="mt-6 text-3xl font-extrabold text-kola">₦{price.toLocaleString("en-NG")}</p><p className="mt-1 text-xs text-muted">6 months from payment · current seller balance: ₦{Number(wallet?.available || 0).toLocaleString("en-NG")}</p><div className="mt-5"><PaidVerificationForm storeId={store.id} price={price} balance={Number(wallet?.available || 0)} currentStatus={currentStatus} purchase={purchaseForForm} /></div></> : <div className="mt-6 rounded-2xl bg-surface p-4 text-sm text-muted">The verification price has not been set yet. Sella Admin will make this available here once configured.</div>}</section><Link href="/dashboard" className="text-sm font-bold text-kola">← Back to overview</Link></div>;
}

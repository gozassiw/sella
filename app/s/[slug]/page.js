import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { cookies } from "next/headers";
import { MapPin, Phone, Store as StoreIcon } from "lucide-react";
import { createClient } from "@/lib/supabase/server";
import { getPublicStore, getPublicProducts } from "@/lib/storefront";
import { formatNaira, whatsappLink } from "@/lib/utils";
import { BRAND } from "@/lib/config";
import CartLink from "@/components/CartLink";
import FollowStoreButton from "@/components/FollowStoreButton";
import ReportForm from "@/components/ReportForm";
import PublicBottomNav from "@/components/PublicBottomNav";
export async function generateMetadata({ params }) { const store = await getPublicStore(params.slug); if (!store) return { title: "Store not found" }; return { title: store.name, description: store.description || `Shop from ${store.name}` }; }
export default async function StorePage({ params }) { const store = await getPublicStore(params.slug); if (!store) notFound(); const products = await getPublicProducts(store.id); const hasSessionCookie = cookies().getAll().some(({ name }) => name.includes("-auth-token")); const supabase = hasSessionCookie ? createClient() : null; const { data: { user } = {} } = supabase ? await supabase.auth.getUser() : { data: {} }; const { data: follow } = user && supabase ? await supabase.from("buyer_store_follows").select("id").eq("user_id", user.id).eq("store_id", store.id).maybeSingle() : { data: null }; const color = store.brand_color || "#1B2A57"; const wa = whatsappLink(store.whatsapp, `Hello ${store.name}, I found your store online.`); return <div className="min-h-screen bg-white pb-28"><header className="text-white" style={{ background: color }}><div className="mx-auto max-w-6xl px-5 pb-10 pt-8"><div className="flex items-start gap-4"><div className="shrink-0">{store.logo_url ? <Image src={store.logo_url} alt={`${store.name} logo`} width={72} height={72} priority className="h-[72px] w-[72px] rounded-2xl bg-white object-cover" /> : <div className="flex h-[72px] w-[72px] items-center justify-center rounded-2xl bg-white/15 text-3xl font-bold">{store.name.charAt(0).toUpperCase()}</div>}</div><div className="min-w-0"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-white/70">Seller storefront</p><h1 className="mt-1 text-3xl font-bold md:text-4xl">{store.name}</h1><p className="mt-2 flex items-start gap-2 text-sm text-white/85"><MapPin size={17} className="mt-0.5 shrink-0" /> <span><strong className="font-semibold">Business address:</strong> {store.address || "Address not provided"}</span></p></div></div>{store.description && <p className="mt-6 max-w-2xl leading-7 text-white/85">{store.description}</p>}<div className="mt-6 flex flex-wrap items-center gap-3">{wa && <a href={wa} target="_blank" rel="noreferrer" className="rounded-full bg-white px-5 py-2.5 text-sm font-semibold" style={{ color }}>Chat on WhatsApp</a>}{store.phone && <a href={`tel:${store.phone}`} className="inline-flex items-center gap-2 rounded-full border border-white/40 px-4 py-2.5 text-sm font-semibold text-white"><Phone size={16} /> Call seller</a>}<FollowStoreButton storeId={store.id} initialFollowing={Boolean(follow)} color={color} returnTo={`/s/${store.slug}`} /></div><div className="mt-4 flex flex-wrap items-center gap-3"><CartLink color={color} /><Link href="/login?next=%2Faccount" className="rounded-full border border-white/40 px-4 py-2 text-sm font-semibold text-white">Buyer login</Link><Link href={`/signup?next=${encodeURIComponent(`/account/setup?returnTo=/s/${store.slug}`)}`} className="rounded-full bg-white px-4 py-2 text-sm font-semibold" style={{ color }}>Create buyer account</Link></div></div></header><main className="mx-auto max-w-6xl px-5 py-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="flex items-center gap-2 text-sm font-semibold text-kola"><StoreIcon size={17} /> {store.category || "Independent seller"}</p><h2 className="mt-1 text-2xl font-bold">Products from {store.name}</h2><p className="mt-1 text-sm text-muted">Browse the seller’s available products below.</p></div><ReportForm storeId={store.id} type="store" triggerLabel="Report this store" /></div>{!products.length ? <div className="py-20 text-center"><p className="text-lg font-semibold">No products available yet</p><p className="mt-2 text-sm text-muted">This seller is still preparing the storefront. Check back shortly.</p></div> : <div className="mt-8 grid grid-cols-2 gap-x-4 gap-y-8 md:grid-cols-3 lg:grid-cols-4">{products.map((product, index) => <article key={product.id} className="min-w-0"><Link href={`/s/${store.slug}/p/${product.id}`} className="group block"><div className="relative aspect-square overflow-hidden rounded-2xl bg-surface">{product.image_urls?.[0] ? <Image src={product.image_urls[0]} alt={product.name} fill priority={index < 2} sizes="(max-width: 768px) 50vw, (max-width: 1200px) 33vw, 25vw" className="object-cover transition group-hover:scale-[1.03]" /> : <div className="flex h-full items-center justify-center text-xs text-muted">No image</div>}{product.stock <= 0 && <span className="absolute left-2 top-2 rounded-full bg-white px-2.5 py-1 text-xs font-semibold">Sold out</span>}</div><p className="mt-3 line-clamp-2 text-sm font-semibold">{product.name}</p><p className="mt-1 font-bold">{formatNaira(product.price)}{product.compare_at_price > product.price && <span className="ml-2 text-sm font-normal text-muted line-through">{formatNaira(product.compare_at_price)}</span>}</p></Link><div className="mt-2"><ReportForm storeId={store.id} productId={product.id} type="product" triggerLabel="Report product" /></div></article>)}</div>}</main><footer className="border-t border-line bg-surface py-10 text-center"><p className="text-sm text-muted">{store.name} · {store.address || "Nigeria"}</p><p className="mt-2 text-xs text-muted">Store powered by <Link href="/" className="font-semibold text-kola">{BRAND}</Link>. Create yours free.</p></footer><PublicBottomNav /></div>; }

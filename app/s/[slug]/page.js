import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { cookies } from "next/headers";
import { MapPin, Store as StoreIcon } from "lucide-react";
import { createClient } from "@/lib/supabase/server";
import { getPublicStore, getPublicProducts } from "@/lib/storefront";
import { formatNaira } from "@/lib/utils";
import { BRAND } from "@/lib/config";
import FollowStoreButton from "@/components/FollowStoreButton";
import ReportForm from "@/components/ReportForm";
import PublicBottomNav from "@/components/PublicBottomNav";
export async function generateMetadata({ params }) { const store = await getPublicStore(params.slug); if (!store) return { title: "Store not found" }; return { title: store.name, description: store.description || `Shop from ${store.name}` }; }
export default async function StorePage({ params }) { const store = await getPublicStore(params.slug); if (!store) notFound(); const products = await getPublicProducts(store.id); const hasSessionCookie = cookies().getAll().some(({ name }) => name.includes("-auth-token")); const supabase = hasSessionCookie ? createClient() : null; const { data: { user } = {} } = supabase ? await supabase.auth.getUser() : { data: {} }; const { data: follow } = user && supabase ? await supabase.from("buyer_store_follows").select("id").eq("user_id", user.id).eq("store_id", store.id).maybeSingle() : { data: null }; const oldColours = ["#0E5E4A", "#1B2A57", "#182243"]; const color = oldColours.includes(store.brand_color) ? "#243A92" : store.brand_color || "#243A92"; return <div className="min-h-screen bg-white pb-28"><header className="text-white" style={{ background: color }}><div className="mx-auto max-w-6xl px-5 pb-10 pt-8"><div className="flex items-start gap-4"><div className="shrink-0">{store.logo_url ? <Image src={store.logo_url} alt={`${store.name} logo`} width={76} height={76} priority className="h-[76px] w-[76px] rounded-3xl bg-white object-cover" /> : <div className="flex h-[76px] w-[76px] items-center justify-center rounded-3xl bg-white/15 text-3xl font-bold">{store.name.charAt(0).toUpperCase()}</div>}</div><div className="min-w-0"><p className="text-xs font-bold uppercase tracking-[0.18em] text-white/70">Seller storefront</p><h1 className="display mt-1 text-4xl font-bold md:text-5xl">{store.name}</h1><p className="mt-3 flex items-start gap-2 text-sm text-white/85"><MapPin size={17} className="mt-0.5 shrink-0" /> <span>{store.address || "Address not provided"}</span></p></div></div>{store.description && <p className="mt-6 max-w-2xl leading-7 text-white/85">{store.description}</p>}<div className="mt-7"><FollowStoreButton storeId={store.id} initialFollowing={Boolean(follow)} color={color} returnTo={`/s/${store.slug}`} /></div></div></header><main className="mx-auto max-w-6xl px-5 py-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="flex items-center gap-2 text-sm font-bold text-kola"><StoreIcon size={17} /> {store.category || "Independent seller"}</p><h2 className="display mt-2 text-3xl font-bold">Products from {store.name}</h2><p className="mt-1 text-sm text-muted">Browse the seller’s available products below.</p></div><ReportForm storeId={store.id} type="store" triggerLabel="Report this store" /></div>{!products.length ? <div className="py-20 text-center"><p className="text-lg font-semibold">No products available yet</p><p className="mt-2 text-sm text-muted">This seller is still preparing the storefront. Check back shortly.</p></div> : <div className="mt-8 grid grid-cols-2 gap-x-4 gap-y-8 md:grid-cols-3 lg:grid-cols-4">{products.map((product, index) => <article key={product.id} className="min-w-0"><Link href={`/s/${store.slug}/p/${product.id}`} className="group block"><div className="relative aspect-square overflow-hidden rounded-3xl bg-surface">{product.image_urls?.[0] ? <Image src={product.image_urls[0]} alt={product.name} fill priority={index < 2} sizes="(max-width: 768px) 50vw, (max-width: 1200px) 33vw, 25vw" className="object-cover transition duration-500 group-hover:scale-[1.04]" /> : <div className="flex h-full items-center justify-center text-xs text-muted">No image</div>}{product.stock <= 0 && <span className="absolute left-2 top-2 rounded-full bg-white px-2.5 py-1 text-xs font-semibold">Sold out</span>}</div><p className="mt-3 line-clamp-2 text-sm font-semibold">{product.name}</p><p className="mt-1 font-bold text-kola">{formatNaira(product.price)}{product.compare_at_price > product.price && <span className="ml-2 text-sm font-normal text-muted line-through">{formatNaira(product.compare_at_price)}</span>}</p></Link><div className="mt-2"><ReportForm storeId={store.id} productId={product.id} type="product" triggerLabel="Report product" /></div></article>)}</div>}</main><footer className="border-t border-line bg-surface py-10 text-center"><p className="text-sm font-semibold text-ink">{store.name}</p><p className="mt-2 text-xs text-muted">Store powered by <Link href="/" className="font-semibold text-kola">{BRAND}</Link>. Create yours free.</p></footer><PublicBottomNav /></div>; }

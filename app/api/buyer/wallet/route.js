import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { generateBuyerWallet } from "@/lib/buyer";
export async function POST() { const supabase = createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return NextResponse.json({ error: "Please log in." }, { status: 401 }); const { data: held } = await supabase.rpc("is_account_held", { p_user_id: user.id }); if (held) return NextResponse.json({ error: "Your account is on hold. Wallet actions are paused while Sella Team reviews it." }, { status: 403 }); try { const wallet = await generateBuyerWallet(user); return NextResponse.json(wallet); } catch (error) { return NextResponse.json({ error: error.message || "Unable to generate a funding account." }, { status: 400 }); } }

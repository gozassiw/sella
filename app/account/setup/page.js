import { redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/auth";
import BuyerProfileForm from "@/components/BuyerProfileForm";
export default async function BuyerSetupPage({ searchParams }) { const { supabase, user } = await getCurrentUser(); const returnTo = typeof searchParams?.returnTo === "string" && searchParams.returnTo.startsWith("/s/") ? searchParams.returnTo : "/account"; if (!user) redirect(`/login?next=${encodeURIComponent(`/account/setup?returnTo=${returnTo}`)}`); const { data: profile } = await supabase.from("buyer_profiles").select("id").eq("user_id", user.id).maybeSingle(); if (profile) redirect(returnTo); return <div className="mx-auto flex min-h-[70vh] max-w-lg items-center"><div className="panel w-full"><p className="text-sm font-semibold text-kola">Buyer setup</p><h1 className="mt-1 text-3xl font-bold">Tell us about you</h1><p className="mt-2 text-sm text-muted">Your full name and WhatsApp number are required so sellers can fulfil your orders and contact you.</p><div className="mt-6"><BuyerProfileForm returnTo={returnTo} /></div></div></div>; }

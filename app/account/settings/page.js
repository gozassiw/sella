import { getCurrentUser } from "@/lib/auth";
export default async function AccountSettingsPage() { const { supabase, user } = await getCurrentUser(); return <div className="mx-auto max-w-2xl space-y-6"><div><h1 className="text-2xl font-bold">Account settings</h1><p className="mt-1 text-sm text-muted">Your single buyer account works across every Sella store.</p></div><section className="panel"><p className="text-sm text-muted">Email</p><p className="mt-1 font-semibold">{user.email}</p><p className="mt-5 text-sm text-muted">To change your email or password, use the authentication settings in the next account release.</p></section><section className="panel"><h2 className="font-semibold">Buyer protection</h2><p className="mt-2 text-sm text-muted">Check the seller’s fulfilment updates and use the report form inside an order if something goes wrong.</p></section></div>; }
